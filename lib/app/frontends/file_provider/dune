(library
 (flags
  (:standard
   -open
   Tsync_stdlib
   -open
   Tsync_core
   -open
   Tsync_checkout
   -open
   Tsync_cache_layout_lwt
   -open
   Tsync_core_lwt
   -open
   Tsync_conf
   -open
   Tsync_conf_lwt
   -open
   Tsync_conf_parsing
   -open
   Tsync_engine
   -open
   Tsync_runtime
   -open
   Tsync_frontend_register
   -open
   Tsync_menu))
 (name tsync_file_provider_frontend)
 (enabled_if
  (= %{system} macosx))
 (foreign_stubs
  (language c)
  (names platform_stubs))
 (foreign_stubs
  (language c)
  (names preview_stubs)
  ; QuickLook's generators are Objective-C, and ARC keeps the request and its
  ; completion block from having to be released by hand.
  (flags
   (-x objective-c -fobjc-arc)))
 (c_library_flags
  (-framework
   QuickLookThumbnailing
   -framework
   ImageIO
   -framework
   CoreGraphics
   -framework
   Foundation))
 (libraries
  tsync_stdlib
  tsync-libs.conf_parsing
  tsync_conf_lwt
  tsync_core
  tsync-libs.menu
  tsync_checkout
  tsync_cache_layout_lwt
  tsync-libs.conf
  tsync-libs.core
  tsync-libs.core_lwt
  tsync-libs.engine
  tsync_runtime
  tsync-libs.frontend_register
  yojson
  base64
  unix
  lwt
  lwt.unix))
