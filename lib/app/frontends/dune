(library
 (name tsync_frontend)
 (library_flags (-linkall))
 (libraries
  tsync_http_proxy_frontend
  (select
   frontend_android.ml
   from
   (tsync-android -> frontend_android.enabled.ml)
   (-> frontend_android.disabled.ml))
  (select
   frontend_fuse.ml
   from
   (tsync_fuse_frontend -> frontend_fuse.enabled.ml)
   (-> frontend_fuse.disabled.ml))
  (select
   frontend_file_provider.ml
   from
   (tsync_file_provider_frontend -> frontend_file_provider.enabled.ml)
   (-> frontend_file_provider.disabled.ml))))
