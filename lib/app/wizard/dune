; The config editor. Its own library because it shares nothing with the rest of
; the CLI — no domain, no store — and is the only thing here that prompts at a
; terminal or shells out to terraform.

(library
 (flags
  (:standard
   -open
   Tsync_stdlib
   -open
   Tsync_core
   -open
   Tsync_conf_parsing
   -open
   Tsync_backend_register
   -open
   Tsync_backend_lwt
   -open
   Tsync_frontend_register
   -open
   Tsync_io_lwt
   -open
   Tsync_tls))
 (name tsync_wizard)
 (libraries
  tsync_stdlib
  tsync_core
  tsync_conf_parsing
  tsync_backend_register
  tsync_backend_lwt
  tsync_frontend_register
  tsync_io_lwt
  tsync_tls
  yojson
  unix))
