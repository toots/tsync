; What this client has changed and not published: the manifest sidecars and the
; bytes they point at. The only copy there is, so a resync leaves this tree
; alone while it rebuilds the rest of the checkout.

(library
 (flags
  (:standard
   -open
   Tsync_id
   -open
   Tsync_conf
   -open
   Tsync_naming
   -open
   Tsync_manifest
   -open
   Tsync_chunks
   -open
   Tsync_chunk_cache
   -open
   Tsync_local_io
   -open
   Tsync_lwt_unix
   -open
   Tsync_backend_register
   -open
   Tsync_bigstring
   -open
   Tsync_log))
 (name tsync_staged)
 (public_name tsync-libs.staged)
 (libraries
  tsync_id
  tsync_conf
  tsync_naming
  tsync_manifest
  tsync_chunks
  tsync_chunk_cache
  tsync_local_io
  tsync_lwt_unix
  tsync_backend_register
  tsync_bigstring
  tsync_log
  yojson
  unix
  lwt
  base64
  lwt.unix))
