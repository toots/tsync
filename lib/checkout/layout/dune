; Where everything under a domain's cache root sits: the manifest tree, the
; chunks, the staged half, and the index from a folder id back to its path.
; Below the things filed there, so each of them can name it.

(library
 (flags
  (:standard
   -open
   Tsync_filename
   -open
   Tsync_logical_key
   -open
   Tsync_chunks
   -open
   Tsync_io_lwt))
 (name tsync_cache_layout)
 (public_name tsync-libs.cache_layout)
 (libraries
  tsync_filename
  tsync_logical_key
  tsync_chunks
  tsync_io_lwt
  unix
  lwt
  lwt.unix))
