; The domain file operations over the manifest mirror, the staged tree and the
; chunk store: what a frontend calls, and the implementation of tsync-libs.file_ops.

(library
 (flags
  (:standard
   -open
   Tsync_logical_key
   -open
   Tsync_history
   -open
   Tsync_remote
   -open
   Tsync_staged
   -open
   Tsync_checkout
   -open
   Tsync_manifests
   -open
   Tsync_file_ops
   -open
   Tsync_wal
   -open
   Tsync_wal_lwt
   -open
   Tsync_manifest
   -open
   Tsync_bigstring
   -open
   Tsync_journal
   -open
   Tsync_conf
   -open
   Tsync_folder_ids
   -open
   Tsync_folder_ids_lwt
   -open
   Tsync_layout
   -open
   Tsync_store
   -open
   Tsync_file_store
   -open
   Tsync_content
   -open
   Tsync_io_lwt
   -open
   Tsync_backend_register
   -open
   Tsync_log))
 (name tsync_file)
 (public_name tsync-libs.file)
 (libraries
  tsync_logical_key
  tsync_history
  tsync_remote
  tsync_staged
  tsync_checkout
  tsync_manifests
  tsync_file_ops
  tsync_wal
  tsync_wal_lwt
  tsync_manifest
  tsync_bigstring
  tsync_journal
  tsync_conf
  tsync_folder_ids
  tsync_folder_ids_lwt
  tsync_layout
  tsync_store
  tsync_file_store
  tsync_content
  tsync_io_lwt
  tsync_backend_register
  tsync_log
  unix
  lwt
  lwt.unix))
