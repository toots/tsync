; This client's working copy of the domain: each published manifest filed under
; the file's real path, and on top of it the staged tree of what this client has
; changed and not published. Rebuildable from the store, apart from that staged
; half, which is the only copy there is.

(library
 (flags
  (:standard
   -open
   Tsync_filename
   -open
   Tsync_logical_key
   -open
   Tsync_staged
   -open
   Tsync_conf
   -open
   Tsync_naming
   -open
   Tsync_manifest
   -open
   Tsync_journal
   -open
   Tsync_durable_queue_lwt
   -open
   Tsync_retry
   -open
   Tsync_chunks
   -open
   Tsync_io_lwt
   -open
   Tsync_backend_register
   -open
   Tsync_bigstring
   -open
   Tsync_log))
 (name tsync_checkout)
 (public_name tsync-libs.checkout)
 (libraries
  tsync-libs.log
  tsync_filename
  tsync_logical_key
  tsync_staged
  tsync_conf
  tsync_naming
  tsync_manifest
  tsync_journal
  tsync_durable_queue_lwt
  tsync_retry
  tsync_chunks
  tsync_io_lwt
  tsync_backend_register
  tsync_bigstring
  yojson
  unix
  lwt
  base64
  lwt.unix))
