(library
 (name tsync_file_provider)
 (wrapped false)
 (enabled_if
  (= %{system} macosx))
 (foreign_stubs
  (language c)
  (names platform_stubs))
 (libraries
  tsync_conf
  tsync_core
  tsync_file
  tsync_queue
  tsync_ipc_handler
  yojson
  unix
  lwt
  lwt.unix))
