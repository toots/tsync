(library
 (name tsync_file_provider)
 (wrapped false)
 (enabled_if
  (= %{system} macosx))
 (foreign_stubs
  (language c)
  (names platform_stubs))
 (foreign_stubs
  (language c)
  (names preview_stubs)
  ; QuickLook's generators are Objective-C, and ARC keeps the request and its
  ; completion block from having to be released by hand.
  (flags
   (-x objective-c -fobjc-arc)))
 (c_library_flags
  (-framework
   QuickLookThumbnailing
   -framework
   ImageIO
   -framework
   CoreGraphics
   -framework
   Foundation))
 (libraries
  tsync_conf
  tsync_core
  tsync_engine
  tsync_file
  tsync_queue
  tsync_ipc_handler
  yojson
  base64
  unix
  lwt
  lwt.unix))
