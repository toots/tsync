; Ids that only need not to collide, and the rarer ones whose unguessability is
; load-bearing. Nothing here knows what an id names.

(library
 (name tsync_id)
 (public_name tsync-libs.id)
 (libraries unix))
