; A domain's manifests on the backend: written, read, moved and deleted under
; whatever key the layout gives them.

(library
 (flags
  (:standard
   -open
   Tsync_logical_key
   -open
   Tsync_conf
   -open
   Tsync_layout
   -open
   Tsync_bigstring
   -open
   Tsync_backend_register
   -open
   Tsync_io))
 (name tsync_store)
 (public_name tsync-libs.store)
 (libraries
  tsync_logical_key
  tsync_conf
  tsync_layout
  tsync_bigstring
  tsync_backend_register
  tsync_io))
