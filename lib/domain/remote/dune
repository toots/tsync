; A domain as its stores hold it: how a logical key becomes a stored one, the
; manifests, versions, journal and cursor under those keys, the chunks a file is
; cut into, and the collection that reclaims them.

(include_subdirs unqualified)

(library
 (flags
  (:standard
   -open
   Tsync_core
   -open
   Tsync_io
   -open
   Tsync_manifest
   -open
   Tsync_conf
   -open
   Tsync_backend_register
   -open
   Tsync_chunk_integrity
   -open
   Tsync_stdlib
   -open
   Tsync_journal
   -open
   Tsync_cache_layout))
 (name tsync_remote)
 (public_name tsync-libs.remote)
 (libraries
  tsync-libs.core
  tsync_core
  tsync_io
  tsync_manifest
  tsync_conf
  tsync_backend_register
  unix
  tsync_chunk_integrity
  tsync_stdlib
  tsync_journal
  yojson
  tsync_cache_layout))
