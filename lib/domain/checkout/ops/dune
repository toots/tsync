; What a frontend can do to a file in a domain, as a signature on its own: the
; things written against it are driven in tests by a stub rather than a domain.

(library
 (flags
  (:standard
   -open
   Tsync_logical_key
   -open
   Tsync_checkout_maintenance
   -open
   Tsync_staged
   -open
   Tsync_manifest
   -open
   Tsync_journal
   -open
   Tsync_checkout
   -open
   Tsync_wal
   -open
   Tsync_bigstring))
 (name tsync_file_ops)
 (public_name tsync-libs.file_ops)
 (libraries
  tsync_logical_key
  tsync_checkout_maintenance
  tsync_staged
  tsync_manifest
  tsync_journal
  tsync_checkout
  tsync_wal
  tsync_bigstring
  unix))
