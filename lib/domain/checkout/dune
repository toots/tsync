; This client's working copy of the domain: the manifest tree, the staged half
; over it, the chunk cache the bytes are read through, and the file operations a
; frontend is handed. One library, because its modules only ever ship together.

(include_subdirs unqualified)

(library
 (flags
  (:standard
   -open
   Tsync_manifest
   -open
   Tsync_conf
   -open
   Tsync_cache_layout
   -open
   Tsync_io
   -open
   Tsync_backend_register
   -open
   Tsync_core
   -open
   Tsync_remote
   -open
   Tsync_stdlib
   -open
   Tsync_journal))
 (name tsync_checkout)
 (public_name tsync-libs.checkout)
 (libraries
  tsync_manifest
  tsync_conf
  tsync_cache_layout
  tsync_io
  tsync_backend_register
  tsync_core
  unix
  tsync_remote
  tsync_stdlib
  tsync_journal
  yojson
  base64))
