; One file's manifest: what the store published for it, what this client staged
; over that, and the writing of either. The tree they sit in is tsync_checkout,
; which is built on this.

(library
 (flags
  (:standard
   -open
   Tsync_logical_key
   -open
   Tsync_staged
   -open
   Tsync_conf
   -open
   Tsync_cache_layout
   -open
   Tsync_manifest
   -open
   Tsync_bigstring
   -open
   Tsync_io))
 (name tsync_manifests)
 (public_name tsync-libs.manifests)
 (libraries
  tsync_logical_key
  tsync_staged
  tsync_conf
  tsync_cache_layout
  tsync_manifest
  tsync_bigstring
  tsync_io
  unix))
