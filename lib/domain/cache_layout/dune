; Where a checkout keeps things on disk, and the index of which directory
; carries which folder id: the leaf the journal and the store reach as well.

(library
 (flags
  (:standard
   -open
   Tsync_filename
   -open
   Tsync_logical_key
   -open
   Tsync_chunks
   -open
   Tsync_io))
 (name tsync_cache_layout)
 (public_name tsync-libs.cache_layout)
 (libraries
  tsync_filename
  tsync_logical_key
  tsync_chunks
  tsync_io
  unix
  yojson))
