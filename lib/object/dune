; The shapes that live in the object store, and their codecs: a file's manifest
; and the chunk table that is its body, the cache-chunk grouping over that
; table, and the journal ops a mutation is recorded as. Data, not machinery --
; nothing here reaches a backend.

(library
 (flags
  (:standard
   -open
   Tsync_conf
   -open
   Tsync_naming
   -open
   Tsync_local_io
   -open
   Tsync_lwt_unix
   -open
   Tsync_xxhash
   -open
   Tsync_chunk
   -open
   Tsync_backend_register
   -open
   Tsync_log))
 (name tsync_object)
 (libraries
  tsync_conf
  tsync_naming
  tsync_local_io
  tsync_lwt_unix
  tsync_xxhash
  tsync_chunk
  tsync_backend_register
  tsync_log
  base64
  yojson
  unix
  lwt
  lwt.unix))
