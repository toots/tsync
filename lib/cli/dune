; Every command the binary offers, one module each, over the shared half in
; Common. Private: it depends on the frontend and backend aggregators, which are
; private so their (select ...) is re-asked on each build of the binary.

(library
 (flags
  (:standard
   -open
   Tsync_logical_key
   -open
   Tsync_chunk_integrity
   -open
   Tsync_history
   -open
   Tsync_layout
   -open
   Tsync_store
   -open
   Tsync_file_store
   -open
   Tsync_corruption
   -open
   Tsync_collection
   -open
   Tsync_staged
   -open
   Tsync_staged_lwt
   -open
   Tsync_checkout
   -open
   Tsync_journal
   -open
   Tsync_field_spec
   -open
   Tsync_sync
   -open
   Tsync_ops
   -open
   Tsync_folder_ids
   -open
   Tsync_folder_ids_lwt
   -open
   Tsync_ipc
   -open
   Tsync_conf_parsing
   -open
   Tsync_wizard
   -open
   Tsync_log
   -open
   Tsync_io_lwt
   -open
   Tsync_metrics
   -open
   Tsync_job_progress
   -open
   Tsync_job_report
   -open
   Tsync_job_report_lwt
   -open
   Tsync_tls
   -open
   Tsync_conf
   -open
   Tsync_bigstring
   -open
   Tsync_backend_register
   -open
   Tsync_domain_store
   -open
   Tsync_domain
   -open
   Tsync_oneshot
   -open
   Tsync_backend
   -open
   Tsync_diagnostics
   -open
   Tsync_frontend_register
   -open
   Tsync_launcher
   -open
   Tsync_frontend
   -open
   Tsync_device
   -open
   Tsync_runtime))
 (name tsync_cli)
 (libraries
  tsync_logical_key
  tsync_chunk_integrity
  tsync_history
  tsync_layout
  tsync_store
  tsync_file_store
  tsync_corruption
  tsync_collection
  tsync_staged
  tsync_staged_lwt
  tsync_checkout
  tsync_journal
  tsync_io_lwt
  tsync-libs.field_spec
  tsync-libs.sync
  tsync-libs.ops
  tsync-libs.store
  tsync_folder_ids
  tsync_folder_ids_lwt
  tsync-libs.frontend_register
  tsync_launcher
  tsync-libs.bigstring
  tsync-libs.backend_register
  tsync-libs.metrics
  tsync-libs.job_progress
  tsync-libs.job_report
  tsync-libs.job_report_lwt
  tsync-libs.conf_parsing
  tsync_wizard
  tsync-libs.ipc
  tsync-libs.conf
  tsync_backend
  tsync-libs.diagnostics
  tsync-libs.domain_store
  tsync_domain
  tsync_oneshot
  tsync-libs.log
  tsync_runtime
  tsync_device
  tsync_frontend
  tsync-libs.tls
  cmdliner
  memtrace
  yojson
  lwt
  lwt.unix
  threads.posix
  unix))
