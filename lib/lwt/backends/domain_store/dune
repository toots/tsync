; A domain's stores as one, on Lwt: the deferred targets behind it and the
; settling a command waits on.

(library
 (flags
  (:standard
   -open
   Tsync_domain_store
   -open
   Tsync_backend_lwt
   -open
   Tsync_durable_queue_lwt
   -open
   Tsync_io_lwt))
 (name tsync_domain_store_lwt)
 (public_name tsync-libs.domain_store_lwt)
 (libraries
  tsync_domain_store
  tsync_backend_lwt
  tsync_durable_queue_lwt
  tsync_io_lwt))
