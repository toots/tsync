; The store side on Lwt: every functor of tsync_remote applied once, so the
; memos and pools keyed per domain are one table each.

(include_subdirs unqualified)

(library
 (flags
  (:standard
   -open
   Tsync_remote
   -open
   Tsync_io_lwt
   -open
   Tsync_conf_lwt
   -open
   Tsync_logical_key
   -open
   Tsync_ipc_lwt
   -open
   Tsync_journal
   -open
   Tsync_bigstring
   -open
   Tsync_cache_layout_lwt
   -open
   Tsync_backend_lwt))
 (name tsync_remote_lwt)
 (public_name tsync-libs.remote_lwt)
 (libraries
  tsync_remote
  tsync_io_lwt
  lwt
  tsync_conf_lwt
  tsync_logical_key
  tsync_ipc_lwt
  tsync_journal
  tsync_bigstring
  tsync_cache_layout_lwt
  tsync_backend_lwt))
