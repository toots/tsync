; The working copy on Lwt: every functor of tsync_checkout applied to this
; process's filesystem, syscalls, pools and clock.

(include_subdirs unqualified)

(library
 (flags
  (:standard
   -open
   Tsync_checkout
   -open
   Tsync_io_lwt
   -open
   Tsync_cache_layout_lwt
   -open
   Tsync_conf_lwt
   -open
   Tsync_remote_lwt
   -open
   Tsync_journal
   -open
   Tsync_core
   -open
   Tsync_core_lwt))
 (name tsync_checkout_lwt)
 (public_name tsync-libs.checkout_lwt)
 (libraries
  tsync_checkout
  tsync_io_lwt
  lwt
  tsync_cache_layout_lwt
  tsync_conf_lwt
  tsync_remote_lwt
  tsync_journal
  tsync_core
  lwt.unix
  unix
  tsync_core_lwt))
