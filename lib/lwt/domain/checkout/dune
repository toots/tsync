; The working copy on Lwt: the manifest tree and the staged half over it, read
; and written through this process's filesystem.

(library
 (flags
  (:standard
   -open
   Tsync_checkout
   -open
   Tsync_manifests_lwt
   -open
   Tsync_staged_lwt
   -open
   Tsync_folder_ids_lwt
   -open
   Tsync_cache_layout_lwt
   -open
   Tsync_io_lwt))
 (name tsync_checkout_lwt)
 (public_name tsync-libs.checkout_lwt)
 (libraries
  tsync_checkout
  tsync_manifests_lwt
  tsync_staged_lwt
  tsync_folder_ids_lwt
  tsync_cache_layout_lwt
  tsync_io_lwt))
