; Synchronising a domain on Lwt: this client's records drained outward, other
; clients' entries applied inward, and the timer that asks.

(library
 (flags
  (:standard
   -open
   Tsync_sync
   -open
   Tsync_remote_lwt
   -open
   Tsync_checkout_lwt
   -open
   Tsync_io_lwt))
 (name tsync_sync_lwt)
 (public_name tsync-libs.sync_lwt)
 (libraries tsync_sync tsync_remote_lwt tsync_checkout_lwt tsync_io_lwt))
