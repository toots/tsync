; The staged tree on Lwt: the unpublished bodies and the manifests over them,
; bound to this process's filesystem.

(library
 (flags
  (:standard
   -open
   Tsync_staged
   -open
   Tsync_cache_layout_lwt
   -open
   Tsync_io_lwt))
 (name tsync_staged_lwt)
 (public_name tsync-libs.staged_lwt)
 (libraries tsync_staged tsync_cache_layout_lwt tsync_io_lwt lwt lwt.unix))
