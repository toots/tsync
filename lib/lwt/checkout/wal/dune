; The intent log on Lwt: the records bound to this process's durable queue, and
; the queue itself for whoever drains them.

(library
 (flags
  (:standard
   -open
   Tsync_wal
   -open
   Tsync_durable_queue_lwt
   -open
   Tsync_io_lwt))
 (name tsync_wal_lwt)
 (public_name tsync-libs.wal_lwt)
 (libraries tsync_wal tsync_durable_queue_lwt tsync_io_lwt lwt))
