(* Empty on purpose: (select ...) in dune needs a file per branch, and choosing
   this one is what puts the library in the link. Which frontends a binary has
   is answered by Frontend.names, not from here. *)
