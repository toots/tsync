; A mutation, recorded so it survives a crash and replayed until every peer
; agrees: this client's intent log, the upload queue that drains it, the shared
; journal read back in both directions, and the per-domain file operations a
; frontend actually calls.

(library
 (flags
  (:standard
   -open
   Tsync_retry
   -open
   Tsync_logical_key
   -open
   Tsync_history
   -open
   Tsync_remote
   -open
   Tsync_staged
   -open
   Tsync_checkout
   -open
   Tsync_manifest
   -open
   Tsync_journal
   -open
   Tsync_conf
   -open
   Tsync_naming
   -open
   Tsync_store
   -open
   Tsync_content
   -open
   Tsync_local_io
   -open
   Tsync_io_lwt
   -open
   Tsync_backend_register
   -open
   Tsync_log
   -open
   Tsync_work))
 (name tsync_sync)
 (public_name tsync-libs.sync)
 (libraries
  tsync_retry
  tsync_logical_key
  tsync_history
  tsync_remote
  tsync_staged
  tsync_checkout
  tsync_manifest
  tsync_journal
  tsync_work
  tsync_conf
  tsync_naming
  tsync_store
  tsync_content
  tsync_local_io
  tsync_io_lwt
  tsync_backend_register
  tsync_log
  unix
  lwt
  lwt.unix))
