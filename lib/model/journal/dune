; What one change to a domain looks like, and the name that follows it from a
; client's own record through the published entry to the cursor a peer compares
; against. Shared: this client writes them and reads other clients'.

(library
 (flags
  (:standard
   -open
   Tsync_id
   -open
   Tsync_conf
   -open
   Tsync_naming
   -open
   Tsync_io_lwt
   -open
   Tsync_log))
 (name tsync_journal)
 (public_name tsync-libs.journal)
 (libraries
  tsync-libs.log
  tsync_id
  tsync_conf
  tsync_naming
  tsync_io
  tsync_io_lwt
  yojson
  unix
  lwt.unix))
