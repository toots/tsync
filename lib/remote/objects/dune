; Uploading a file's bytes and fetching them back: what a store does for one
; object, in logical keys. Above the store's naming and below the read path
; that calls it.

(library
 (flags
  (:standard
   -open
   Tsync_logical_key
   -open
   Tsync_history
   -open
   Tsync_staged
   -open
   Tsync_chunk_cache
   -open
   Tsync_checkout
   -open
   Tsync_manifest
   -open
   Tsync_journal
   -open
   Tsync_conf
   -open
   Tsync_naming
   -open
   Tsync_chunks
   -open
   Tsync_chunk_store
   -open
   Tsync_store
   -open
   Tsync_local_io
   -open
   Tsync_lwt_unix
   -open
   Tsync_xxhash
   -open
   Tsync_metrics
   -open
   Tsync_bigstring
   -open
   Tsync_backend_register
   -open
   Tsync_log))
 (name tsync_remote)
 (public_name tsync-libs.remote)
 (libraries
  tsync_logical_key
  tsync_history
  tsync_staged
  tsync_chunk_cache
  tsync_checkout
  tsync_manifest
  tsync_journal
  tsync_conf
  tsync_naming
  tsync_chunks
  tsync_chunk_store
  tsync_store
  tsync_local_io
  tsync_lwt_unix
  tsync_xxhash
  tsync_metrics
  tsync_bigstring
  tsync_backend_register
  tsync_log
  unix
  lwt))
