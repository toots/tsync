; What the store keeps of things the live tree stopped pointing at: a file's
; earlier manifests. The key shape is built and taken apart here, in one place,
; since the two must agree.

(library
 (flags
  (:standard
   -open
   Tsync_logical_key
   -open
   Tsync_conf
   -open
   Tsync_conf_lwt
   -open
   Tsync_layout
   -open
   Tsync_layout_lwt
   -open
   Tsync_bigstring
   -open
   Tsync_backend_register
   -open
   Tsync_backend_lwt
   -open
   Tsync_log))
 (name tsync_history)
 (public_name tsync-libs.history)
 (libraries
  tsync_logical_key
  tsync_conf
  tsync_conf_lwt
  tsync_layout
  tsync_layout_lwt
  tsync_bigstring
  tsync_backend_register
  tsync_backend_lwt
  tsync_log
  lwt
  unix))
