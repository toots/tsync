; Which backend key a logical key maps to: the inode scheme, where a folder's
; id names the space its children sit in, and the identity scheme for a caller
; already holding backend keys.

(library
 (flags
  (:standard
   -open
   Tsync_logical_key
   -open
   Tsync_folder_ids
   -open
   Tsync_folder_ids_lwt
   -open
   Tsync_conf
   -open
   Tsync_conf_lwt
   -open
   Tsync_bigstring
   -open
   Tsync_backend_register
   -open
   Tsync_backend_lwt
   -open
   Tsync_log))
 (name tsync_layout)
 (public_name tsync-libs.layout)
 (libraries
  tsync_logical_key
  tsync_folder_ids
  tsync_folder_ids_lwt
  tsync_conf
  tsync_conf_lwt
  tsync_bigstring
  tsync_backend_register
  tsync_backend_lwt
  tsync_log
  lwt
  unix))
