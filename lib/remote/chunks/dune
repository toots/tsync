; Keeping chunks in a store: naming bytes, and not sending what is already
; there. Scoped under remote because a chunk's name is what makes either
; question answerable, and both answers are about what the store holds.

(library
 (flags
  (:standard
   -open
   Tsync_chunks
   -open
   Tsync_bigstring
   -open
   Tsync_lwt_unix
   -open
   Tsync_metrics
   -open
   Tsync_logical_key))
 (name tsync_chunk_store)
 (public_name tsync-libs.chunk_store)
 (libraries
  tsync-libs.logical_key
  tsync_chunks
  tsync_bigstring
  tsync_lwt_unix
  tsync_metrics
  lwt))
