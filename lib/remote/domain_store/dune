; One write fanned across the backends a domain is configured with, and a
; durable record of what an unreachable one still owes. Policy over drivers
; rather than a driver, which is why it sits here and not in lib/backends.

(library
 (flags
  (:standard
   -open
   Tsync_chunks
   -open
   Tsync_naming
   -open
   Tsync_log
   -open
   Tsync_lwt_unix
   -open
   Tsync_local_io
   -open
   Tsync_bigstring
   -open
   Tsync_backend_register
   -open
   Tsync_work))
 (name tsync_domain_store)
 (public_name tsync-libs.domain_store)
 (libraries
  tsync_chunks
  tsync_naming
  tsync_work
  tsync_bigstring
  tsync_backend_register
  tsync_local_io
  tsync_lwt_unix
  tsync_log
  lwt
  unix
  lwt.unix
  yojson))
