# SPM / macOS build
.build/
.build-xcode/

# OCaml / dune
_build/
compile_commands.json

# Debian packages from linux/Dockerfile
dist/

# Xcode (generated by xcodegen from project.yml)
*.xcodeproj/
!*.xcodeproj/xcshareddata/xcschemes/

# Xcode user data
xcuserdata/
*.xcuserstate
*.xccheckout
*.moved-aside

# macOS
.DS_Store

# Terraform
.terraform/
# Provider lock: not committed — its h1 hashes are per-platform and churn across
# contributors' OSes, and each operator provisions their own project/state.
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
!example.tfvars
backend.hcl
# Only the backend-*.tf.example templates are committed; the activated one
# (whichever backend you chose) is local.
backend-s3.tf
backend-gcs.tf
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform/build/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

dist/
