(executable
 (flags
  (:standard -open Tsync_backend_register -open Tsync_gcs_backend))
 (name gcs)
 (libraries tsync_gcs_backend tsync_backend_register))

(alias
 (name default)
 (deps gcs.exe))

(rule
 (alias runtest)
 (action
  (run ./gcs.exe)))
