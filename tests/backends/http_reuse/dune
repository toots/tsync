(executable
 (flags
  (:standard
   -open
   Tsync_bigstring
   -open
   Tsync_http_client
   -open
   Tsync_retry
   -open
   Tsync_test_harness))
 (name http_reuse)
 (libraries
  cohttp-lwt
  cohttp
  tsync_test_harness
  tsync_bigstring
  tsync_http_client
  tsync_retry
  cohttp-lwt-unix
  uri
  lwt
  lwt.unix
  unix))
