(executable
 (flags
  (:standard
   -open
   Tsync_remote
   -open
   Tsync_remote_lwt
   -open
   Tsync_test_harness))
 (name dedup_test)
 (libraries tsync_test_harness tsync_remote tsync_remote_lwt lwt lwt.unix))
