; [tsync_local_backend] is linked for its registration alone: a member needs a
; store module, and capacity reads only the fields beside it.
;
; [tsync_metrics] for the one assertion that spans both: what [human_bytes]
; prints is what [configure] offers back as a default, so it has to parse.

(test
 (flags
  (:standard
   -open
   Tsync_conf_parsing
   -open
   Tsync_metrics
   -open
   Tsync_conf
   -open
   Tsync_backend_register
   -open
   Tsync_local_io))
 (name conf_test)
 (libraries
  unix
  tsync_conf_parsing
  tsync_metrics
  tsync-libs.conf
  tsync-libs.backend_register
  tsync_local_backend
  tsync-libs.local_io))
