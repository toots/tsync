; [tsync_local_backend_lwt] is linked for its registration alone: a member needs a
; store module, and capacity reads only the fields beside it.
;
; [tsync_metrics] for the one assertion that spans both: what [human_bytes]
; prints is what [configure] offers back as a default, so it has to parse.

(test
 (flags
  (:standard
   -open
   Tsync_conf_parsing
   -open
   Tsync_metrics
   -open
   Tsync_conf
   -open
   Tsync_backend_register
   -open
   Tsync_backend_lwt
   -open
   Tsync_io_lwt))
 (name conf_test)
 (libraries
  unix
  tsync_conf_parsing
  tsync_metrics
  tsync-libs.conf
  tsync-libs.backend_register
  tsync-libs.backend_lwt
  tsync_local_backend_lwt
  tsync_io_lwt))
