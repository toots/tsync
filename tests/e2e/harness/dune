; Shared by the per-platform end-to-end programs. No frontend dependency: a
; frontend gives the user a directory, and what is asserted about that directory
; is the same either way.

(library
 (flags
  (:standard -open Tsync_core -open Tsync_runtime))
 (name tsync_e2e)
 (libraries tsync_core tsync_runtime yojson unix threads.posix))
