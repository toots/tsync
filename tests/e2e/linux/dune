; Not a `test`: it mounts a filesystem. Run by hand with `make -C linux e2e`.

(executable
 (flags
  (:standard -open Tsync_e2e))
 (name e2e_linux)
 (enabled_if
  (= %{system} linux))
 (libraries tsync_e2e yojson unix))
