; Not a `test`: it needs the app installed, and it restarts it and registers a
; domain. Run by hand with `make -C macos e2e`.

(executable
 (name e2e_macos)
 (enabled_if
  (= %{system} macosx))
 (libraries
  tsync_e2e
  tsync_core
  tsync_runtime
  tsync_file_provider
  yojson
  unix))
