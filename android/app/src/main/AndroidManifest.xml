<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Camera backup reads the roll. READ_MEDIA_VISUAL_USER_SELECTED is
         declared so "Select photos" is a state the app can recognise and say
         out loud, rather than a camera roll that silently looks nearly empty.

         ACCESS_MEDIA_LOCATION only permits the GPS tags; keeping them also
         needs MediaStore.setRequireOriginal on the URI being opened. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- extractNativeLibs: without it nativeLibraryDir points inside the APK and
         there is no file on disk to exec. Pairs with useLegacyPackaging in the
         build script; both are required.

         usesCleartextTraffic: the daemon is a native binary and reaches a
         plain-http proxy on a LAN whatever the platform thinks. Blocking only
         the setup form's check would refuse a server that then works. -->
    <application
        android:label="tsync"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- work-runtime declares this service without a foregroundServiceType,
             so BackupWorker asking for dataSync is not a subset of it and the
             platform throws on the service's own thread, past any catch the
             worker can hold. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- A descriptor's reads are answered in this process, and the system
             freezes a cached one: this holds the process up for as long as a
             file the app is serving is open. -->
        <service
            android:name=".OpenDocumentsService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- MANAGE_DOCUMENTS is held by the system only, so this is reachable
             through the picker rather than by arbitrary apps. -->
        <provider
            android:name=".TsyncProvider"
            android:authorities="org.feverdreamtv.tsync.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

    </application>
</manifest>
